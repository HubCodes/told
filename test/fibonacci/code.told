use fio;
use string;

def fibonacci(n: int): int {
	if (n ~ 1 | n ~ 2) {
		return 1;
	}
	return fibonacci(n - 1) + fibonacci(n - 2);
}

def main(): int {
	puti(fibonacci(10));
	putc('\n');
	putstrln("good result.");
	return 0;
}
